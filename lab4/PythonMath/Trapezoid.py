import math

def Area(a,b,c):
    print("Expected Output: ",( (a+b) / 2) * c)

height = float(input("Height: "))
first = float(input("Base, first value: "))
second = float(input("Base, second value: "))

Area(first, second, height)

